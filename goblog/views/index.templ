package views

import (
    "amritanshu.in/goblog/md"
    "fmt"
)

templ Index(posts map[string]md.Post) {
    @Base() {
        <h3>All posts:</h3>
        for p := range posts {
            <l1><a href={templ.SafeURL(fmt.Sprintf("/article/%s", p))}>{p}</a></l1><br>
        }
    }
}